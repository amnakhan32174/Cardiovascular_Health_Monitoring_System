# PCG Model Server Dependencies
# Compatible with Python 3.11+ (numpy 1.21.x requires Python <3.11)
# Install: pip install -r pcg_requirements.txt

# Core - pin scipy to version with pre-built Windows wheels (avoids OpenBLAS build)
numpy>=1.24.0,<2
# scipy pulled in by librosa; install separately if needed: pip install numpy scipy
scipy==1.11.4

# Deep Learning - tf_keras (Keras 2) for legacy .h5 with TFOpLambda support
tensorflow>=2.10.0
tf-keras>=2.16.0

# Audio/Signal - MFCC extraction
librosa>=0.10.0
soundfile>=0.12.0

# Excel - TestLabel.xlsx
openpyxl>=3.0.0
pandas>=1.5.0

# API (match main requirements.txt)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
